<h1>Reports#report_by_dates</h1>
<p>Find me in app/views/reports/report_by_dates.html.erb</p>

<% @report.each do |date, sum| %>
    <ul>
        <li>
            <span><%= date.strftime('%d/%m') %>:</span>
            <span><%= sum %></span>
        </li>
    </ul>
<% end %>
